# CMakeLists.txt for the DaylightMap plugin
SET(DAYLIGHTMAP_VERSION "${VERSION}")

ADD_DEFINITIONS(-DDAYLIGHTMAP_PLUGIN_VERSION="${DAYLIGHTMAP_VERSION}")
ADD_DEFINITIONS(-DDAYLIGHTMAP_PLUGIN_LICENSE="GNU GPLv2 or later")

ADD_SUBDIRECTORY(src)
